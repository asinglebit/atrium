/// Escapes a string for use inside a JSON string literal. atrium emits exactly
/// one fixed-shape document -- Claude's `--settings` -- so this is cheaper than
/// taking on a serialiser to interpolate a path.
pub fn escape(value: &str) -> String {
    let mut out = String::with_capacity(value.len());
    for c in value.chars() {
        match c {
            '"' => out.push_str("\\\""),
            '\\' => out.push_str("\\\\"),
            '\n' => out.push_str("\\n"),
            '\r' => out.push_str("\\r"),
            '\t' => out.push_str("\\t"),
            c if (c as u32) < 0x20 => out.push_str(&format!("\\u{:04x}", c as u32)),
            c => out.push(c),
        }
    }
    out
}

#[cfg(test)]
#[path = "../tests/helpers/json.rs"]
mod tests;
